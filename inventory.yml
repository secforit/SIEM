---
all:
  hosts:
    siem-server:
      ansible_host: 10.2.3.5
      ansible_user: razvan
      ansible_python_interpreter: /usr/bin/python3
      ansible_ssh_private_key_file: "~/.ssh/id_rsa"

  children:
    wazuh_managers:
      hosts:
        siem-server:
    wazuh_indexers:
      hosts:
        siem-server:
    monitoring:
      hosts:
        siem-server:
