---
all:
  hosts:
    siem-server:
      ansible_host: 127.0.0.1  # Change to your server IP
      ansible_connection: local  # Remove this line for remote deployment
      ansible_python_interpreter: /usr/bin/python3

  children:
    wazuh_managers:
      hosts:
        siem-server:
    wazuh_indexers:
      hosts:
        siem-server:
    monitoring:
      hosts:
        siem-server:
