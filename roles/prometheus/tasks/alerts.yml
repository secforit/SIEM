---
# ============================================================
# Prometheus Alert Rules
# ============================================================

- name: Deploy alert rules
  ansible.builtin.template:
    src: alert_rules.yml.j2
    dest: "{{ prometheus_config_dir }}/rules/siem_alerts.yml"
    owner: prometheus
    group: prometheus
    mode: "0644"
  notify: reload prometheus
